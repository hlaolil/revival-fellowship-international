<%- include('partials/header', { activePage: 'ministries' }) %>

<section class="hero relative h-96 bg-cover bg-center" style="background-image: url('/images/heroo.webp')">
  <div class="absolute inset-0 bg-black bg-opacity-60 flex items-center justify-center">
    <h1 class="text-5xl md:text-7xl font-bold text-white font-heading">Our Ministries</h1>
  </div>
</section>

<div class="max-w-7xl mx-auto px-6 py-16">
  <% if (ministries && ministries.length > 0) { %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-10">

      <% ministries.forEach(ministry => { %>
        <%
          let name = ministry.name.toLowerCase();
          let webImageUrl = '';

          // Worship Ministry
          if (name.includes('worship')) {
            webImageUrl = '/images/worship.jpg';

          // Youth Ministry
          } else if (name.includes('youth')) {
            webImageUrl = '/images/youth.jpg';

          // Children Ministry
          } else if (
            name.includes('children') ||
            name.includes('sunday school') ||
            name.includes('kids')
          ) {
            webImageUrl = '/images/children.jpg';

          // Prayer Ministry
          } else if (name.includes('prayer')) {
            webImageUrl = '/images/prayer.jpg';

          // Outreach Ministry
          } else if (
            name.includes('outreach') ||
            name.includes('community') ||
            name.includes('service')
          ) {
            webImageUrl = '/images/outreach.jpg';

          // Bible Study
          } else if (
            name.includes('bible') ||
            name.includes('study')
          ) {
            webImageUrl = '/images/bible-study.jpg';

          // Choir / Music
          } else if (
            name.includes('choir') ||
            name.includes('music')
          ) {
            webImageUrl = '/images/choir.jpg';

          // -------- WOMEN'S MINISTRY (LOCAL IMAGE) --------
          } else if (
            name.includes('women’s') ||   // curly apostrophe
            name.includes("women's") ||   // straight apostrophe
            name.includes('womens') ||
            name.includes('women') ||
            name.includes('lady') ||
            name.includes('ladies') ||
            name.includes('woman')
          ) {
            webImageUrl = '/images/womens-ministry.jpg';

          // Men's Ministry
          } else if (name.includes('men')) {
            webImageUrl = '/images/men.jpg';
          }
        %>

        <div class="bg-white rounded-xl overflow-hidden shadow-xl hover:shadow-2xl transition transform hover:-translate-y-4">

          <!-- ALWAYS use default category image -->
          <img 
            src="<%= webImageUrl ? webImageUrl : '/images/no-image.jpg' %>" 
            alt="<%= ministry.name %>" 
            class="w-full h-48 object-cover"
          >

          <div class="p-6">
            <h2 class="text-2xl font-bold text-primary mb-4 text-center"><%= ministry.name %></h2>
            <p class="text-center"><strong>Schedule:</strong> <%= ministry.schedule %></p>
            <p class="text-center mt-4"><strong>Contact:</strong> <%= ministry.contact %></p>
          </div>
        </div>

      <% }) %>
    </div>
  <% } else { %>
    <p class="text-center text-2xl text-gray-600 py-20">No ministries listed yet. Check back soon!</p>
  <% } %>
</div>

<%- include('partials/footer') %>

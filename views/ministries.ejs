<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles/ministries.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <%- include('partials/header') %>

  <main class="discover-section">
    <h1>Our Ministries</h1>

    <% if (ministries && ministries.length > 0) { %>
      <div class="grid-container">
        <% ministries.forEach(ministry => { %>
          <div class="card place-card">
            <% if (ministry.image) { %>
              <figure>
                <img 
                  src="/images/ministries/<%= ministry.image %>" 
                  alt="<%= ministry.name %>"
                  loading="lazy"
                >
              </figure>
            <% } else { %>
              <figure style="background:#eee;height:200px;display:flex;align-items:center;justify-content:center;color:#999;">
                No Image
              </figure>
            <% } %>

            <h2><%= ministry.name %></h2>
            <p><strong>Schedule:</strong> <%= ministry.schedule %></p>
            <p><strong>Contact:</strong> <%= ministry.contact %></p>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p style="text-align:center;">No ministries listed yet. Check back soon!</p>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>
</html>

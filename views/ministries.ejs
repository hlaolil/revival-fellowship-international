<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles/ministries.css">
</head>
<body>
  <%- include('partials/header') %>

  <main class="container">
    <h1>Our Ministries</h1>

    <% if (ministries && ministries.length > 0) { %>
      <div class="ministries-grid">
        <% ministries.forEach(ministry => { %>
          <article class="ministry-card">
            <% if (ministry.image) { %>
              <img 
                src="/images/ministries/<%= ministry.image %>" 
                alt="<%= ministry.name %>"
                loading="lazy"
              >
            <% } else { %>
              <div class="placeholder-img">Image</div>
            <% } %>

            <h2><%= ministry.name %></h2>
            <p><strong>Schedule:</strong> <%= ministry.schedule %></p>
            <p><strong>Contact:</strong> <%= ministry.contact %></p>
          </article>
        <% }) %>
      </div>
    <% } else { %>
      <p>No ministries available at the moment.</p>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
